{
  "id": "codex-wrapper-updater",
  "name": "Wrapper update button",
  "description": "Adds a separate top-right Update button that appears when a newer Codex Desktop Linux wrapper build (this project's own Linux features and fixes) is available. One click rebuilds and restarts. Distinct from the upstream Codex app updater. Requires the 'Check for Codex Desktop Linux updates' setting to be enabled.",
  "entrypoints": {
    "patches": "./patch.js",
    "stageHook": "./stage.sh"
  }
}
